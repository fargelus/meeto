<h1><%= t('authorize') %></h1>

<%= form_with url: authorize_path do |f| %>
  <div class="row">
    <div class="col-12">
      <%= f.text_field 'user[email]', class: 'form-control', placeholder: 'Email' %>
    </div>

    <div class="col-12 mt-3">
      <%= f.password_field 'user[password]', class: 'form-control', placeholder: t('auth.passwd') %>
    </div>

    <div>
      <%= f.submit t('auth.login'), class: 'btn btn-primary mt-3' %>
    </div>
  </div>
<% end %>

<%= render partial: 'partials/notice', locals: { text: flash.notice, category: 'alert-danger' } %>
